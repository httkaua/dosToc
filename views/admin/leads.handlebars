<h4>Clientes</h4>
<a class="btn btn-dark" href="/admin/leads/new-lead">Adicionar lead</a>
<table class="table table-hover mt-4">
  <thead>
    <tr>
        <th class="bg-primary text-white">Nome</th>
        <th class="bg-primary text-white">Telefone</th>
        <th class="bg-primary text-white">Status</th>
        <th class="bg-primary text-white">Opções</th>
    </tr>
  </thead>
  <tbody>
    {{#each lead}}
    <tr>
        <td>{{this.name}}</td>
        <td>{{this.phone}}</td>
        <td>{{this.status}}</td>
        <td>
          <div class="d-grid gap-2 d-md-block">
            <button class="btn btn-success" type="button">Agendar tarefa</button>
            <a href="/admin/leads/{{this.id}}" class="btn btn-info">Detalhes</a>
            <a href="/admin/leads/{{this.id}}/hidden" class="btn btn-danger">🗑</a>
          </div>
        </td>
    </tr>
    {{/each}}
  </tbody>
</table>
<a href="/admin" class="btn btn-secondary">Voltar</a>
