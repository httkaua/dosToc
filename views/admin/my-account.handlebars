<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-lg">
        <div class="card-header bg-primary text-white text-center">
          <h5>Minha Conta</h5>
        </div>
        <div class="card-body">
          <form method="POST" action="/admin/my-account/{{user.userID}}/update">
            <div class="mb-3">
              <label for="firstName" class="form-label">Nome</label>
              <input
                type="text"
                class="form-control"
                id="firstName"
                name="firstName"
                placeholder="Seu nome"
                value= "{{user.firstName}}"
              />
            </div>
            <div class="mb-3">
              <label for="lastName" class="form-label">Sobrenome</label>
              <input
                type="text"
                class="form-control"
                id="lastName"
                name="lastName"
                placeholder="Seu sobrenome"
                value="{{user.lastName}}"
              />
            </div>
            <div class="mb-3">
              <label for="phone" class="form-label">Telefone</label>
              <input
                type="tel"
                class="form-control"
                id="phone"
                name="phone"
                placeholder="Seu telefone"
                value="{{user.phone}}"
              />
            </div>
            <div class="mb-3">
              <label for="company" class="form-label">Empresa(s)</label>
              <input
                type="text"
                class="form-control"
                id="company"
                name="company"
                placeholder="Sua(s) empresa(s)"
                value="{{user.company}}"
                disabled
              />
            </div>
            <div class="mb-3">
              <label for="email" class="form-label">Email</label>
              <input
                type="email"
                class="form-control"
                id="email"
                name="email"
                placeholder="Seu email"
                value="{{user.email}}"
              />
              <small>Caso queira alterar seu e-mail, seu gestor deverá autorizar a alteração para confirmar. Você será notificado.</small>
            </div>
            <div class="mb-3">
              <label for="password" class="form-label">Senha</label>
              <input
                type="email"
                class="form-control"
                id="password"
                name="password"
                placeholder="************"
                value="************"
                disabled
              />
              <a href="/user/forgot-password" class="btn btn-danger mt-2">Esqueci minha senha</a>
            </div>
            
            <div class="d-flex justify-content-end mt-4">
              <a href="/admin" class="btn btn-secondary me-2">Voltar</a>
              <button type="submit" class="btn btn-success">
                Salvar
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
